1.

2. class Cats {
    string color;
    int age;

    public static void Cats( string color, int age){

    }
}

3. The variable references

4. MyCounter counter = new Mycounter();
    MyCounter = counter;
    counter = new MyCounter

5. public static double myMeth(int a, int b);

6. return value;

7. The same name as the class

8. Creates a new object.

9. Garbage collection is javas auto cleanup to free up
    memory that is no longer being used. finalize() is used to ensure that an object terminates cleanly.

10. this is used to specify the object that the method is calling

11. Yes

12. void